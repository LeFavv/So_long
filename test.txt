itoa --> passer de l'int pour le nombre de mouvements a une str
calculer la len de la string pour savoir le nombre a afficher
if len == 3
print_moves (je pars du principe que max 1000 mouvs sont effectues)
if (str[0] == 0)
flag = 0;
else flag += 1;
ne rien afficher
if (str[1] && flag == 1)
afficher le nombre
else (flag = 0)
if (str[2] && flag == 1)
afficher le nombre
if (str[3] && flag == 1)
afficher le nombre

chercher des images de nombres un peu pixel sympas et mettre ca dans une struct
appeler cette struct pour afficher la bonne image au bon moment
une fonction d'affichage

